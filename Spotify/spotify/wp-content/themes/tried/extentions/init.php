<?php

defined('ABSPATH') || exit;

add_action( 'init', 'extentions_init' );
function extentions_init() {
    // $extentions = array(
    //     array(
    //         'path' => '/extentions/baohanh/baohanh.php',
    //         'class' => 'Tried_EXT__Baohanh'
    //     )
    // );
    // if ( !empty( $extentions ) ) {
    //     foreach ( $extentions as $extention ) {
    //         if ( !class_exists( $extention['class'] ) ) {
    //             include_once get_template_directory() . $extention['path'];
    //         }
    //     }
    // }
}